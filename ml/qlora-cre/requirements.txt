# QLoRA learning track — pinned to a known-good API surface (2024/2025-era).
# The TRL/PEFT APIs move fast; if you bump these, re-check the SFTTrainer /
# SFTConfig signature used in train_qlora.py.
#
# Train/eval require a CUDA GPU. prepare_dataset.py runs on CPU.

torch>=2.3.0
transformers==4.44.2
datasets==2.21.0
accelerate==0.33.0
peft==0.12.0
trl==0.9.6
bitsandbytes==0.43.3
huggingface_hub>=0.24.0
pyyaml>=6.0

# Only needed for `prepare_dataset.py --source db` (reads your Postgres logs).
psycopg[binary]>=3.1
